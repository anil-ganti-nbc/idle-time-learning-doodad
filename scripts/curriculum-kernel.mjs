/**
 * Operating systems, networking, compilers.
 * Authoring only — runtime loads the generated JSON.
 */
export function kernelCourses(concept) {
  return [
    {
      id: "os-foundations",
      title: "Operating Systems Foundations",
      categoryId: "os",
      description: "Kernel, syscalls, processes, and isolation — the contract between program and machine, taught the xv6 way: small enough to hold.",
      curriculumVersion: 1,
      orderHint: 0,
      difficultyRange: [0, 2],
      entryRequirements: [],
      sourceReferences: [
        { id: "mit-61810", title: "MIT 6.1810 Operating System Engineering", url: "https://ocw.mit.edu/courses/6-1810-operating-system-engineering-fall-2023/pages/syllabus/", kind: "ocw", notes: "Hardware/software contract first. xv6 as a complete small Unix." },
        { id: "mit-xv6", title: "xv6 book", url: "https://pdos.csail.mit.edu/6.1810/2025/xv6.html", kind: "notes", notes: "Organization, traps, isolation before locks and filesystems." },
      ],
      modules: [
        { id: "os-f-kernel", title: "The kernel as a program", blurb: "Privilege, traps, syscalls.", order: 0, prerequisites: [], spineIds: ["os-kernel", "os-syscall"], learningObjectives: ["Treat the kernel as the program that owns the machine", "Name privilege, traps, and syscalls as the crossing"] },
        { id: "os-f-proc", title: "Processes", blurb: "Address spaces, fork, isolation.", order: 1, prerequisites: ["os-f-kernel"], spineIds: ["os-process"], learningObjectives: ["Treat a process as an isolated address space", "Use fork, exec, and wait as the process API"] },
        { id: "os-f-switch", title: "Leaving and returning", blurb: "Interrupts, context switch, devices at a glance.", order: 2, prerequisites: ["os-f-proc"], spineIds: ["os-context-switch"], learningObjectives: ["Explain a context switch as saving one life and restoring another", "Treat interrupts as devices entering the kernel"] },
      ],
      concepts: [
        concept("os-kernel", "The kernel", "os-f-kernel", 0, [], "The program that owns the machine and rents it out.", { src: ["mit-61810", "mit-xv6"], obj: ["Explain the kernel as the program that owns the machine and rents it to user programs", "Name what the kernel mediates that a lone program cannot safely do itself"] }),
        concept("os-privilege", "Privilege rings", "os-f-kernel", 1, ["os-kernel"], "User code that cannot issue the dangerous instructions.", { src: ["mit-61810", "mit-xv6"], parent: "os-kernel", obj: ["Distinguish privileged instructions from ordinary user-mode execution", "Predict what happens when user code tries a privileged operation"] }),
        concept("os-syscall", "System calls", "os-f-kernel", 1, ["os-kernel"], "The narrow door a process uses to ask for work.", { src: ["mit-61810", "mit-xv6"], parent: "os-kernel", obj: ["Describe a system call as the narrow door a program uses to request kernel work", "Trace user request, privilege crossing, kernel action, and return"] }),
        concept("os-trap", "Traps and exceptions", "os-f-kernel", 1, ["os-syscall"], "How control transfers into the kernel without a CALL.", { src: ["mit-61810", "mit-xv6"], parent: "os-syscall", obj: ["Explain a trap as a controlled transfer into the kernel that is not an ordinary CALL", "Distinguish a requested syscall trap from an unexpected exception"] }),
        concept("os-user-kernel", "User versus kernel memory", "os-f-kernel", 1, ["os-privilege"], "Two maps, one CPU, a lot of checking.", { src: ["mit-61810", "mit-xv6"], parent: "os-privilege", obj: ["Contrast user-accessible memory with memory the kernel keeps for itself", "Predict why the kernel must check pointers a program hands it"] }),
        concept("os-abi", "The syscall ABI", "os-f-kernel", 2, ["os-syscall"], "Registers, numbers, and the compatibility tax.", { src: ["mit-61810", "mit-xv6"], parent: "os-syscall", obj: ["Describe the syscall ABI as the register and number contract both sides honour", "Predict why changing that contract breaks old binaries"] }),
        concept("os-xv6-map", "A small Unix as a map", "os-f-kernel", 1, ["os-kernel"], "xv6 as a complete system you can finish reading.", { src: ["mit-61810", "mit-xv6"], parent: "os-kernel", obj: ["Treat xv6 as a complete small Unix you can finish reading", "Locate kernel, syscalls, and processes on that map without needing later chapters"] }),
        concept("os-boot", "Booting", "os-f-kernel", 2, ["os-kernel"], "From reset vector to the first process.", { src: ["mit-61810", "mit-xv6"], parent: "os-kernel", obj: ["Trace the machine from reset to a kernel that can run a first process", "Name what must exist before the first user program can start"] }),
        concept("os-process", "Processes and threads", "os-f-proc", 1, ["os-kernel"], "Address spaces, threads, and what isolation actually costs.", { src: ["mit-61810", "mit-xv6"], parent: "os-kernel", obj: ["Describe a process as an address space plus the threads that run in it", "Name the isolation a process is supposed to provide, and that it is not free"] }),
        concept("os-thread-vs-proc", "Threads versus processes", "os-f-proc", 1, ["os-process"], "Shared address space versus a private one.", { src: ["mit-61810", "mit-xv6"], parent: "os-process", obj: ["Contrast threads that share an address space with processes that do not", "Predict what a wild write in one thread can do to its siblings"] }),
        concept("os-isolation", "Isolation", "os-f-proc", 1, ["os-process", "os-privilege"], "A crash next door should be someone else's problem.", { src: ["mit-61810", "mit-xv6"], parent: "os-process", obj: ["Explain isolation as a crash next door remaining someone else's problem", "Diagnose a simple isolation failure when two programs share more than they should"] }),
        concept("os-fork", "fork", "os-f-proc", 2, ["os-process"], "Copy the process, then diverge.", { src: ["mit-61810", "mit-xv6"], parent: "os-process", obj: ["Describe fork as copying a process so parent and child can then diverge", "Predict which state the child starts with and how the two then differ"] }),
        concept("os-exec", "exec", "os-f-proc", 2, ["os-fork"], "Replace the address space, keep the process id.", { src: ["mit-61810", "mit-xv6"], parent: "os-fork", obj: ["Describe exec as replacing the address space while keeping the process identity", "Predict what survives exec and what is overwritten"] }),
        concept("os-wait", "wait", "os-f-proc", 2, ["os-fork"], "Reaping children so they do not stay zombies.", { src: ["mit-61810", "mit-xv6"], parent: "os-fork", obj: ["Explain wait as reaping a child so it does not remain a zombie", "Predict what happens to a dead child if no parent waits"] }),
        concept("os-shell", "The shell as a user of the API", "os-f-proc", 2, ["os-exec", "os-wait"], "A loop of fork, exec, wait that invents scripting.", { src: ["mit-61810", "mit-xv6"], parent: "os-exec", obj: ["Describe the shell as a user program looping fork, exec, and wait", "Predict how that loop becomes a script of commands"] }),
        concept("os-protection", "Protection domains", "os-f-proc", 2, ["os-isolation"], "Who may touch which object.", { src: ["mit-61810", "mit-xv6"], parent: "os-isolation", obj: ["Describe a protection domain as a rule about who may touch which object", "Predict a denial when a process touches an object outside its domain"] }),
        concept("os-monolithic-vs-micro", "Kernel structure", "os-f-proc", 2, ["os-kernel"], "Monoliths, microkernels, and the argument about where the driver lives.", { src: ["mit-61810", "mit-xv6"], parent: "os-kernel", obj: ["Contrast a kernel that contains drivers with one that keeps them in user space", "Predict what a driver crash costs in each structure"] }),
        concept("os-context-switch", "Context switches", "os-f-switch", 2, ["os-process"], "Saving one life and restoring another.", { src: ["mit-61810", "mit-xv6"], parent: "os-process", obj: ["Describe a context switch as saving one process's CPU state and restoring another's", "Predict what must be saved for the first process to resume correctly"] }),
        concept("os-interrupt", "Interrupts", "os-f-switch", 2, ["os-trap"], "The device rings; the kernel answers.", { src: ["mit-61810", "mit-xv6"], parent: "os-trap", obj: ["Describe an interrupt as a device forcing a transfer into the kernel", "Contrast an interrupt with a trap the current program requested"] }),
        concept("os-device-intro", "Devices as files of a sort", "os-f-switch", 2, ["os-interrupt"], "A register file on the other side of a bus.", { src: ["mit-61810", "mit-xv6"], parent: "os-interrupt", obj: ["Describe a device as registers on a bus that the kernel mediates", "Explain why programs usually see devices through file-like handles rather than raw registers"] }),
        concept("os-ipc-intro", "IPC, first look", "os-f-switch", 2, ["os-process"], "Pipes and messages before shared-memory races.", { src: ["mit-61810", "mit-xv6"], parent: "os-process", obj: ["Describe IPC as how processes exchange data without sharing an address space", "Contrast a pipe or message with two processes writing the same memory"] }),
        concept("os-batch-vs-time", "Batch, multiprogramming, time-share", "os-f-switch", 1, ["os-process"], "Why we started switching at all.", { src: ["mit-61810", "mit-xv6"], parent: "os-process", obj: ["Contrast batch, multiprogramming, and time-sharing as reasons to switch", "Predict why a machine started switching instead of running one job to completion"] }),
      ],
    },
    {
      id: "os-concurrency",
      title: "Memory, Concurrency, and Scheduling",
      categoryId: "os",
      description: "Page tables, the TLB, locks, races, and the scheduler that lies about how many CPUs you have.",
      curriculumVersion: 1,
      orderHint: 1,
      difficultyRange: [2, 4],
      entryRequirements: ["os-process", "os-syscall"],
      sourceReferences: [
        { id: "mit-xv6", title: "xv6 book", url: "https://pdos.csail.mit.edu/6.1810/2025/xv6.html", kind: "notes", notes: "Page tables, then locks, then scheduling." },
        { id: "mit-61810", title: "MIT 6.1810", url: "https://ocw.mit.edu/courses/6-1810-operating-system-engineering-fall-2023/pages/syllabus/", kind: "ocw", notes: "Labs extend VM and concurrency after the kernel is thinkable." },
      ],
      modules: [
        { id: "os-c-vm", title: "Virtual memory", blurb: "Pages, tables, faults.", order: 0, prerequisites: [], spineIds: ["os-vm"], learningObjectives: ["Treat an address as a translation the hardware walks", "Treat a missing translation as a request, not only a crash"] },
        { id: "os-c-sched", title: "Scheduling", blurb: "Fairness, latency, and classes.", order: 1, prerequisites: ["os-c-vm"], spineIds: ["os-sched"], learningObjectives: ["Choose which runnable thread owns a core next", "Separate fair share from deadlines"] },
        { id: "os-c-sync", title: "Synchronization", blurb: "Locks, races, atomics.", order: 2, prerequisites: ["os-c-sched"], spineIds: ["os-lock", "os-race"], learningObjectives: ["Name a race as order-dependent results", "Use a lock as one holder at a time"] },
      ],
      concepts: [
        concept("os-vm", "Virtual memory", "os-c-vm", 2, ["os-process"], "Pages, page tables, and the TLB as a cache of translations.", { src: ["mit-61810", "mit-xv6"], obj: ["Describe virtual memory as translating process addresses to physical frames", "Predict what a program sees when two processes use the same virtual address"] }),
        concept("os-page-table", "Page tables", "os-c-vm", 2, ["os-vm"], "A tree of translations the hardware walks.", { src: ["mit-61810", "mit-xv6"], parent: "os-vm", obj: ["Describe a page table as the tree the hardware walks to translate a page", "Predict why a multi-level table can stay sparse"] }),
        concept("os-tlb-os", "TLB shootdown", "os-c-vm", 3, ["os-vm", "cpu-tlb"], "When one core changes a mapping, the others must forget.", { src: ["mit-61810", "mit-xv6"], parent: "os-vm", obj: ["Explain a TLB shootdown as forcing other cores to drop a stale translation", "Predict what happens if one core changes a mapping and the others keep the old TLB entry"] }),
        concept("os-page-fault", "Page faults", "os-c-vm", 2, ["os-vm", "os-trap"], "A missing translation as a request, not only a crash.", { src: ["mit-61810", "mit-xv6"], parent: "os-vm", obj: ["Describe a page fault as a missing translation that transfers into the kernel", "Distinguish a fault that should fill a page from one that should kill the process"] }),
        concept("os-swap", "Paging to disk", "os-c-vm", 3, ["os-page-fault"], "The illusion of more RAM than you bought.", { src: ["mit-61810", "mit-xv6"], parent: "os-page-fault", obj: ["Explain paging to disk as evicting a frame and bringing it back on the next fault", "Predict the cost of treating the disk as extra RAM when many processes miss"] }),
        concept("os-cow", "Copy-on-write", "os-c-vm", 3, ["os-fork", "os-page-fault"], "fork that does not copy until a write.", { src: ["mit-61810", "mit-xv6"], parent: "os-page-fault", obj: ["Describe copy-on-write as sharing frames after fork until a write", "Predict which mapping changes when one side writes a shared page"] }),
        concept("os-mmap", "mmap", "os-c-vm", 3, ["os-vm"], "A file, or anonymous memory, appearing as a range of addresses.", { src: ["mit-61810", "mit-xv6"], parent: "os-vm", obj: ["Describe mmap as making a file or anonymous memory appear as a range of addresses", "Predict what a load from that range does before the bytes are in RAM"] }),
        concept("os-sched", "CPU scheduling", "os-c-sched", 2, ["os-process"], "Fairness, latency, and why CFS is not a real-time scheduler.", { src: ["mit-61810", "mit-xv6"], obj: ["Describe the scheduler as choosing which runnable thread owns a core next", "Contrast a fair-share policy with one that tries to meet a deadline"] }),
        concept("os-fairness", "Fair-share scheduling", "os-c-sched", 3, ["os-sched"], "Who is owed the next slice.", { src: ["mit-61810", "mit-xv6"], parent: "os-sched", obj: ["Explain fair-share as each task being owed a slice weighted by its share", "Predict who runs next when one task has consumed more than its share"] }),
        concept("os-latency-sched", "Latency versus throughput in the scheduler", "os-c-sched", 3, ["os-sched", "arch-latency-throughput"], "Interactive tails versus batch work.", { src: ["mit-61810", "mit-xv6"], parent: "os-sched", obj: ["Contrast scheduler choices that cut wakeup latency with those that maximize throughput", "Predict why a batch job can wreck an interactive tail"] }),
        concept("os-realtime", "Real-time scheduling", "os-c-sched", 4, ["os-sched"], "Deadlines, not averages.", { src: ["mit-61810", "mit-xv6"], parent: "os-sched", obj: ["Describe real-time scheduling as meeting deadlines, not averages", "Predict why a fair-share scheduler is the wrong tool for a deadline"] }),
        concept("os-scheduler-class", "Scheduling classes", "os-c-sched", 3, ["os-sched"], "More than one policy living in one kernel.", { src: ["mit-61810", "mit-xv6"], parent: "os-sched", obj: ["Describe scheduling classes as more than one policy living in one kernel", "Predict which class wins when a real-time task and a fair-share task are both runnable"] }),
        concept("os-load-balance", "Load balancing", "os-c-sched", 3, ["os-sched"], "Moving work across cores without wrecking caches.", { src: ["mit-61810", "mit-xv6"], parent: "os-sched", obj: ["Describe load balancing as moving work across cores", "Predict the cache cost of bouncing a thread to a colder core"] }),
        concept("os-numa", "NUMA awareness", "os-c-sched", 4, ["os-load-balance", "os-vm"], "Memory that is closer to some cores than others.", { src: ["mit-61810", "mit-xv6"], parent: "os-load-balance", obj: ["Explain NUMA as memory that is closer to some cores than others", "Predict why running a thread far from its pages costs more than a local miss"] }),
        concept("os-preempt", "Preemption", "os-c-sched", 2, ["os-context-switch", "os-sched"], "Taking the CPU back.", { src: ["mit-61810", "mit-xv6"], parent: "os-sched", obj: ["Describe preemption as taking the CPU back from a running thread", "Predict what must be saved when the kernel interrupts a thread that did not yield"] }),
        concept("os-priority-inv", "Priority inversion", "os-c-sched", 4, ["os-sched", "os-lock"], "A low task holding a lock a high task needs.", { src: ["mit-61810", "mit-xv6"], parent: "os-sched", obj: ["Diagnose priority inversion as a low task holding a lock a high task needs", "Predict a fix that lets the low task finish without the high task waiting behind a middle one"] }),
        concept("os-lock", "Locks", "os-c-sync", 2, ["os-process"], "Mutual exclusion as a primitive, not a vibe.", { src: ["mit-61810", "mit-xv6"], obj: ["Describe a lock as mutual exclusion: one holder at a time", "Predict what goes wrong if two threads update the same structure without a lock"] }),
        concept("os-sleep-lock", "Sleeping locks", "os-c-sync", 3, ["os-lock"], "Blocking instead of spinning when the hold is long.", { src: ["mit-61810", "mit-xv6"], parent: "os-lock", obj: ["Contrast a sleeping lock with spinning while the holder runs", "Predict when spinning wastes the core and sleeping is cheaper"] }),
        concept("os-deadlock", "Deadlock", "os-c-sync", 3, ["os-lock"], "Everyone waiting for someone who is waiting for them.", { src: ["mit-61810", "mit-xv6"], parent: "os-lock", obj: ["Describe deadlock as everyone waiting for someone who is waiting for them", "Diagnose a two-lock cycle from the order two threads acquire"] }),
        concept("os-race", "Races", "os-c-sync", 2, ["os-thread-vs-proc"], "A result that depends on who ran first.", { src: ["mit-61810", "mit-xv6"], obj: ["Describe a race as a result that depends on who ran first", "Diagnose a lost update when two threads read-modify-write the same word"] }),
        concept("os-atomic", "Atomics", "os-c-sync", 3, ["os-race"], "Read-modify-write that does not tear.", { src: ["mit-61810", "mit-xv6"], parent: "os-race", obj: ["Describe an atomic as a read-modify-write that does not tear", "Predict which updates still race even if each store is atomic"] }),
        concept("os-seqcst", "Memory ordering for programmers", "os-c-sync", 4, ["os-atomic", "cpu-consistency"], "Why a store on this core is not yet a load on that one.", { src: ["mit-61810", "mit-xv6"], parent: "os-atomic", obj: ["Explain why a store on this core is not yet a load on that one", "Predict a reordering that surprises a program that assumed a single global order"] }),
        concept("os-semaphore", "Semaphores", "os-c-sync", 3, ["os-lock"], "A counter you can wait on.", { src: ["mit-61810", "mit-xv6"], parent: "os-lock", obj: ["Describe a semaphore as a counter you can wait on", "Predict how a semaphore of count N lets N holders through"] }),
        concept("os-condvar", "Condition variables", "os-c-sync", 3, ["os-sleep-lock"], "Sleep until someone says the world changed.", { src: ["mit-61810", "mit-xv6"], parent: "os-sleep-lock", obj: ["Describe a condition variable as sleeping until someone says the world changed", "Predict why waiting on a condvar must re-check the condition after wakeup"] }),
      ],
    },
    {
      id: "os-storage",
      title: "Storage, Filesystems, and Kernel Internals",
      categoryId: "os",
      description: "Persistence after virtualization: blocks, inodes, journals, drivers, and the namespaces containers actually use.",
      curriculumVersion: 1,
      orderHint: 2,
      difficultyRange: [2, 5],
      entryRequirements: ["os-vm", "os-lock"],
      sourceReferences: [
        { id: "mit-xv6", title: "xv6 book", url: "https://pdos.csail.mit.edu/6.1810/2025/xv6.html", kind: "notes", notes: "File system after locks. Crash consistency is the hard part." },
        { id: "mit-61810", title: "MIT 6.1810", url: "https://ocw.mit.edu/courses/6-1810-operating-system-engineering-fall-2023/pages/syllabus/", kind: "ocw", notes: "Labs add sophisticated VM and networking onto a known kernel." },
      ],
      modules: [
        { id: "os-s-fs", title: "Filesystems", blurb: "Inodes, directories, journals.", order: 0, prerequisites: [], spineIds: ["os-inode", "os-journal"], learningObjectives: ["Treat a filename as a pointer to an inode", "Write intent before applying it to the home location"] },
        { id: "os-s-io", title: "I/O path", blurb: "Caches, DMA, drivers.", order: 1, prerequisites: ["os-s-fs"], spineIds: ["os-buffer-cache", "os-driver"], learningObjectives: ["Keep recently used blocks in RAM", "Let a device move bytes while the CPU does other work"] },
        { id: "os-s-isol", title: "Modern isolation extras", blurb: "Namespaces, cgroups, virtualization.", order: 2, prerequisites: ["os-s-io"], spineIds: ["os-namespace", "os-container"], learningObjectives: ["Restrict what a process can see of the machine", "Compose namespaces and limits without inventing a new kernel"] },
      ],
      concepts: [
        concept("os-block-dev", "Block devices", "os-s-fs", 2, ["os-device-intro"], "Fixed-size sectors and the fiction of a linear disk.", { src: ["mit-61810", "mit-xv6"], obj: ["Describe a block device as a linear array of fixed-size sectors", "Predict why the filesystem, not the program, chooses which sector to write"] }),
        concept("os-inode", "Inodes", "os-s-fs", 2, ["os-block-dev"], "The on-disk object a filename eventually names.", { src: ["mit-61810", "mit-xv6"], parent: "os-block-dev", obj: ["Describe an inode as the on-disk object a filename eventually names", "Distinguish the inode's metadata and data blocks from the name that points at it"] }),
        concept("os-dir", "Directories", "os-s-fs", 2, ["os-inode"], "A list of names to inode numbers.", { src: ["mit-61810", "mit-xv6"], parent: "os-inode", obj: ["Describe a directory as a list of names to inode numbers", "Predict what two names for one inode means"] }),
        concept("os-fs-layout", "Filesystem layout", "os-s-fs", 3, ["os-inode"], "Superblock, bitmaps, and where the data lives.", { src: ["mit-61810", "mit-xv6"], parent: "os-inode", obj: ["Locate superblock, free-space map, inodes, and data on a disk image", "Predict what is lost if the free-space map is wrong after a crash"] }),
        concept("os-journal", "Journaling", "os-s-fs", 3, ["os-fs-layout"], "Write the intent before the damage.", { src: ["mit-61810", "mit-xv6"], parent: "os-fs-layout", obj: ["Describe journaling as writing the intent before applying it to the home location", "Predict why the journal must hit disk before the metadata it describes"] }),
        concept("os-crash-consist", "Crash consistency", "os-s-fs", 4, ["os-journal"], "A power cut in the middle of rename.", { src: ["mit-61810", "mit-xv6"], parent: "os-journal", obj: ["Diagnose a filesystem after a power cut in the middle of rename", "Predict which states a journaled rename may legally show after replay"] }),
        concept("os-vfs", "The VFS", "os-s-fs", 3, ["os-inode"], "One API over many filesystem types.", { src: ["mit-61810", "mit-xv6"], parent: "os-inode", obj: ["Describe the VFS as one API over many filesystem types", "Predict why two mounts can implement the same open/read/write differently"] }),
        concept("os-mount", "Mounts", "os-s-fs", 3, ["os-vfs"], "Grafting a tree onto a directory.", { src: ["mit-61810", "mit-xv6"], parent: "os-vfs", obj: ["Describe a mount as grafting another tree onto a directory", "Predict which filesystem serves a path after a mount covers that directory"] }),
        concept("os-buffer-cache", "Buffer cache", "os-s-io", 3, ["os-block-dev"], "Recently used blocks kept in RAM.", { src: ["mit-61810", "mit-xv6"], parent: "os-block-dev", obj: ["Describe the buffer cache as recently used blocks kept in RAM", "Predict why a write that hits the cache is not yet durable"] }),
        concept("os-page-cache", "Page cache", "os-s-io", 3, ["os-mmap", "os-buffer-cache"], "File contents as pages, not just blocks.", { src: ["mit-61810", "mit-xv6"], parent: "os-buffer-cache", obj: ["Describe the page cache as file contents kept as pages, not just device blocks", "Contrast a page-cache hit on a mapped file with a block read from the device"] }),
        concept("os-dma", "DMA", "os-s-io", 3, ["os-device-intro"], "The device writes memory while the CPU does something else.", { src: ["mit-61810", "mit-xv6"], obj: ["Describe DMA as the device writing memory while the CPU does other work", "Predict why the CPU must not use a buffer until the device says the transfer is done"] }),
        concept("os-driver", "Device drivers", "os-s-io", 3, ["os-interrupt", "os-dma"], "The kernel code that speaks a particular device's dialect.", { src: ["mit-61810", "mit-xv6"], parent: "os-dma", obj: ["Describe a driver as kernel code that speaks one device's dialect", "Predict what the rest of the kernel sees after the driver hides the device registers"] }),
        concept("os-virtio", "Virtio", "os-s-io", 4, ["os-driver"], "A device model designed to be virtualized.", { src: ["mit-61810", "mit-xv6"], parent: "os-driver", obj: ["Describe virtio as a device model designed to be emulated or paravirtualized", "Predict why a guest driver talking virtio is cheaper than emulating a real NIC"] }),
        concept("os-init", "init and userspace bring-up", "os-s-io", 2, ["os-exec"], "The first process and everything it starts.", { src: ["mit-61810", "mit-xv6"], obj: ["Describe init as the first user process that starts everything else", "Predict what happens to the machine if init exits"] }),
        concept("os-namespace", "Namespaces", "os-s-isol", 3, ["os-isolation"], "A process that cannot see the whole machine.", { src: ["mit-61810", "mit-xv6"], obj: ["Describe a namespace as a process that cannot see the whole machine", "Predict what a process in a PID namespace believes is process 1"] }),
        concept("os-namespace-linux", "Linux namespace types", "os-s-isol", 4, ["os-namespace"], "PID, mount, net, user — different lies.", { src: ["mit-61810", "mit-xv6"], parent: "os-namespace", obj: ["Distinguish PID, mount, net, and user namespaces as different restricted views", "Predict which namespace hides the host's network interfaces"] }),
        concept("os-cgroup", "cgroups", "os-s-isol", 4, ["os-sched"], "Accounting and limits on CPU, memory, and I/O.", { src: ["mit-61810", "mit-xv6"], obj: ["Describe a cgroup as accounting and limits on CPU, memory, and I/O", "Predict what happens when a group hits its memory limit"] }),
        concept("os-capability", "Capabilities", "os-s-isol", 4, ["os-privilege"], "Splitting root into bits you can drop.", { src: ["mit-61810", "mit-xv6"], obj: ["Describe capabilities as splitting root into bits you can drop", "Predict a denial when a process has dropped the bit a privileged operation needs"] }),
        concept("os-seccomp", "seccomp", "os-s-isol", 4, ["os-syscall"], "A filter on which syscalls remain legal.", { src: ["mit-61810", "mit-xv6"], obj: ["Describe seccomp as a filter on which syscalls remain legal", "Predict why an allowed open can still be a sandbox hole"] }),
        concept("os-syscall-filter", "Reducing the syscall surface", "os-s-isol", 4, ["os-seccomp"], "Why a sandboxed process still needs open(2).", { src: ["mit-61810", "mit-xv6"], parent: "os-seccomp", obj: ["Explain why a sandboxed process still needs a few syscalls to do useful work", "Predict the tradeoff between a tiny syscall surface and a working program"] }),
        concept("os-kvm-intro", "KVM at a glance", "os-s-isol", 4, ["os-privilege", "cpu-virtual-addr"], "The kernel as a hypervisor for other kernels.", { src: ["mit-61810", "mit-xv6"], obj: ["Describe KVM as the host kernel running other kernels in a less-privileged mode", "Predict what a privileged instruction in the guest causes on the host"] }),
        concept("os-container", "Containers", "os-s-isol", 4, ["os-namespace", "os-cgroup"], "Namespaces plus cgroups plus a filesystem view — not a VM.", { src: ["mit-61810", "mit-xv6"], parent: "os-namespace", obj: ["Describe a container as namespaces plus cgroups plus a filesystem view", "Contrast a container with a virtual machine that has its own kernel"] }),
      ],
    },
    {
      id: "net-foundations",
      title: "Networking Foundations",
      categoryId: "networking",
      description: "Packets, layers as contracts, Ethernet, IP, and the socket a program actually sees. Routing politics wait.",
      curriculumVersion: 1,
      orderHint: 0,
      difficultyRange: [0, 2],
      entryRequirements: [],
      sourceReferences: [
        { id: "stanford-cs144", title: "Stanford CS144", url: "https://web.stanford.edu/class/cs144/", kind: "syllabus", notes: "Packets and a reliable byte stream before BGP. Layers as implementation contracts." },
        { id: "saltzer-e2e", title: "End-to-end arguments in system design", url: "https://web.mit.edu/Saltzer/www/publications/endtoend/endtoend.pdf", kind: "notes", notes: "Functions that only the endpoints can implement correctly." },
        { id: "rfc791", title: "Internet Protocol", url: "https://www.rfc-editor.org/rfc/rfc791", kind: "notes", notes: "IPv4 addressing, TTL, fragmentation. Mechanism, not RFC trivia." },
        { id: "rfc1034", title: "Domain Names — Concepts and Facilities", url: "https://www.rfc-editor.org/rfc/rfc1034", kind: "notes", notes: "Names that resolve to addresses. The system depth waits." },
      ],
      modules: [
        { id: "net-f-pack", title: "Packets and layers", blurb: "Why we slice the problem.", order: 0, prerequisites: [], spineIds: ["net-stack", "net-packet"], learningObjectives: ["Treat layers as contracts, not a morality play", "Treat a packet as a header plus a payload that can be lost alone"] },
        { id: "net-f-link", title: "Link and local delivery", blurb: "MAC, switching, ARP.", order: 1, prerequisites: ["net-f-pack"], spineIds: ["net-ethernet"], learningObjectives: ["Deliver a frame on a local medium using MAC addresses", "Resolve an IP to a MAC before sending on Ethernet"] },
        { id: "net-f-ip", title: "Internet Protocol", blurb: "Addresses, CIDR, ICMP.", order: 2, prerequisites: ["net-f-link"], spineIds: ["net-ip"], learningObjectives: ["Treat an IPv4 address as a hierarchical name routers forward on", "Use a prefix length, not a class, to decide the network"] },
        { id: "net-f-host", title: "The host interface", blurb: "Sockets, UDP, names.", order: 3, prerequisites: ["net-f-ip"], spineIds: ["net-socket-api"], learningObjectives: ["Bind a port and exchange bytes through the socket API", "Resolve a name and borrow an address before a program can talk"] },
      ],
      concepts: [
        concept("net-stack", "The network stack", "net-f-pack", 0, [], "Layers as contracts, not a morality play.", { src: ["stanford-cs144"], obj: ["Explain a layer as a contract about what the layer above does not have to know", "Name which job belongs to the link, the internet, and the transport, without treating the stack as a morality play"] }),
        concept("net-packet", "Packets", "net-f-pack", 0, ["net-stack"], "A header plus a payload, lost independently of its neighbors.", { src: ["stanford-cs144"], obj: ["Describe a packet as a header plus a payload that can be lost independently of its neighbors", "Predict what a receiver can and cannot assume about the next packet"] }),
        concept("net-layering", "Why layering", "net-f-pack", 1, ["net-stack"], "Each layer offers a service and hides a mechanism.", { src: ["stanford-cs144"], obj: ["Explain that each layer offers a service and hides a mechanism", "Distinguish a service a layer promises from the mechanism it uses to keep that promise"] }),
        concept("net-end-to-end", "The end-to-end argument", "net-f-pack", 2, ["net-layering"], "Functions that only the endpoints can implement correctly.", { src: ["stanford-cs144", "saltzer-e2e"], obj: ["State the end-to-end argument: some functions can only be implemented correctly at the endpoints", "Predict why putting reliability only in the network still leaves the endpoints checking"] }),
        concept("net-bandwidth-delay", "Bandwidth-delay product", "net-f-pack", 2, ["net-packet", "arch-latency-throughput"], "How much data fits in the pipe.", { src: ["stanford-cs144"], obj: ["Define the bandwidth-delay product as how much data fits in the pipe", "Predict why a sender must keep that much data in flight to fill a long fat path"] }),
        concept("net-ethernet", "Ethernet frames", "net-f-link", 1, ["net-packet"], "A local delivery envelope with a MAC address.", { src: ["stanford-cs144"], obj: ["Describe an Ethernet frame as a local delivery envelope with MAC addresses", "Distinguish a frame that stays on a local link from a packet that can leave the network"] }),
        concept("net-mac", "MAC addresses", "net-f-link", 1, ["net-ethernet"], "A burned-in name that is not an Internet name.", { src: ["stanford-cs144"], obj: ["Explain a MAC address as a local hardware name that is not an Internet name", "Predict why two hosts on different networks can reuse the same MAC without colliding on the Internet"] }),
        concept("net-switching", "Switching", "net-f-link", 2, ["net-ethernet"], "Learning which port a MAC last spoke from.", { src: ["stanford-cs144"], obj: ["Describe a switch as learning which port a MAC last spoke from", "Predict which port a frame takes once the switch has seen the destination speak"] }),
        concept("net-arp", "ARP", "net-f-link", 2, ["net-mac"], "Who has this IP? Tell this MAC.", { src: ["stanford-cs144"], obj: ["Explain ARP as asking who has this IP and learning the MAC that answers", "Predict why a host must resolve the next hop's MAC before it can send an IP packet on Ethernet"] }),
        concept("net-wifi-vs-wired", "Shared versus switched media", "net-f-link", 2, ["net-ethernet"], "Collision domains did not vanish; they moved.", { src: ["stanford-cs144"], obj: ["Contrast a shared medium where stations contend with a switched link that isolates collisions", "Predict why a collision domain can still exist on Wi-Fi after Ethernet became switched"] }),
        concept("net-ip", "IPv4 addressing", "net-f-ip", 1, ["net-packet"], "A hierarchical name the routers understand.", { src: ["stanford-cs144", "rfc791"], obj: ["Describe an IPv4 address as a hierarchical name routers use to forward", "Trace how a packet's destination address, not a MAC, decides the next network hop"] }),
        concept("net-cidr", "CIDR and prefixes", "net-f-ip", 2, ["net-ip"], "A length, not a class.", { src: ["stanford-cs144", "rfc791"], obj: ["Explain a prefix as an address plus a length, not a class", "Apply a prefix length to decide whether two addresses share a network"] }),
        concept("net-icmp", "ICMP", "net-f-ip", 2, ["net-ip"], "Control messages: unreachable, too big, echo.", { src: ["stanford-cs144", "rfc791"], obj: ["Describe ICMP as control messages about reachability, size, and echo", "Predict which ICMP message a hop sends when it cannot forward or the packet is too big"] }),
        concept("net-mtu", "MTU", "net-f-ip", 2, ["net-ip"], "The largest packet this hop will accept.", { src: ["stanford-cs144", "rfc791"], obj: ["Define the MTU as the largest packet this hop will accept", "Predict what happens when a packet is larger than the next hop's MTU"] }),
        concept("net-fragment", "Fragmentation", "net-f-ip", 3, ["net-mtu"], "Splitting a packet the next link will not take — and why we try not to.", { src: ["stanford-cs144", "rfc791"], obj: ["Explain fragmentation as splitting a packet the next link will not take", "Predict why we try to avoid fragmenting and what fails if a fragment is lost"] }),
        concept("net-nat-intro", "NAT, first look", "net-f-ip", 2, ["net-ip"], "Many private addresses sharing one public one.", { src: ["stanford-cs144"], obj: ["Describe NAT as many private addresses sharing one public address via a mapping", "Predict why an unsolicited inbound packet has no table entry to land on"] }),
        concept("net-endian-wire", "Network byte order", "net-f-ip", 1, ["net-packet", "cpu-endian"], "Big-endian on the wire, whatever you are at home.", { src: ["stanford-cs144"], obj: ["Explain network byte order as big-endian on the wire regardless of the host", "Predict what a little-endian host must do before a multi-byte header field is sent"] }),
        concept("net-checksum", "Checksums", "net-f-ip", 2, ["net-packet"], "A cheap detector, not a cryptographic proof.", { src: ["stanford-cs144"], obj: ["Describe a checksum as a cheap detector of accidental change, not a cryptographic proof", "Predict what a receiver does when the checksum does not match"] }),
        concept("net-udp", "UDP", "net-f-host", 1, ["net-ip"], "A port number and a hope.", { src: ["stanford-cs144"], obj: ["Describe UDP as a port number on top of IP with no delivery promise", "Predict what an application must do itself if a UDP datagram is lost"] }),
        concept("net-socket-api", "The socket API", "net-f-host", 1, ["net-udp"], "How a process binds a port and exchanges bytes.", { src: ["stanford-cs144"], obj: ["Describe the socket API as how a process binds a port and exchanges bytes", "Trace bind, send, and receive as the process-visible interface to the stack"] }),
        concept("net-dns-intro", "DNS, first look", "net-f-host", 2, ["net-udp"], "Names that resolve to addresses.", { src: ["stanford-cs144", "rfc1034"], obj: ["Explain DNS as a name that resolves to an address", "Predict why a program that only has a name must ask before it can send an IP packet"] }),
        concept("net-dhcp", "DHCP", "net-f-host", 2, ["net-ip"], "Borrowing an address for a while.", { src: ["stanford-cs144"], obj: ["Describe DHCP as borrowing an address and related config for a lease", "Predict what a host lacks if it never hears a DHCP offer"] }),
      ],
    },
    {
      id: "net-transport",
      title: "Transport and Congestion",
      categoryId: "networking",
      description: "Reliability, windows, and how senders share a pipe they cannot see. TCP first; QUIC as a later design that learned the same lessons.",
      curriculumVersion: 1,
      orderHint: 1,
      difficultyRange: [2, 4],
      entryRequirements: ["net-ip", "net-socket-api"],
      sourceReferences: [
        { id: "stanford-cs144", title: "Stanford CS144", url: "https://web.stanford.edu/class/cs144/", kind: "syllabus", notes: "Labs build a reliable transport. Congestion is a mechanism, not a slogan." },
        { id: "rfc9293", title: "Transmission Control Protocol", url: "https://www.rfc-editor.org/rfc/rfc9293", kind: "notes", notes: "Connection state, handshake, windows, retransmission." },
        { id: "rfc5681", title: "TCP Congestion Control", url: "https://www.rfc-editor.org/rfc/rfc5681", kind: "notes", notes: "Slow start, AIMD, fast retransmit as sender mechanisms." },
        { id: "rfc9000", title: "QUIC", url: "https://www.rfc-editor.org/rfc/rfc9000", kind: "notes", notes: "Transport over UDP so the protocol can change without the kernel." },
      ],
      modules: [
        { id: "net-t-rel", title: "Reliability", blurb: "Acks, windows, state.", order: 0, prerequisites: [], spineIds: ["net-reliable", "net-tcp-state"], learningObjectives: ["Retransmit what was lost so a byte stream can be rebuilt", "Treat TCP states and windows as mechanism, not a flag list"] },
        { id: "net-t-cc", title: "Congestion control", blurb: "AIMD, RTT, the unseen pipe.", order: 1, prerequisites: ["net-t-rel"], spineIds: ["net-congestion"], learningObjectives: ["Share a pipe the sender cannot see", "Separate congestion of the path from a receiver that is full"] },
        { id: "net-t-mod", title: "Modern transports", blurb: "Bufferbloat, BBR, QUIC.", order: 2, prerequisites: ["net-t-cc"], spineIds: ["net-bbr", "net-quic"], learningObjectives: ["Treat delay, marks, and user-space transports as later answers to the same problem", "Name the middlebox as part of the path"] },
      ],
      concepts: [
        concept("net-reliable", "Reliable delivery", "net-t-rel", 2, ["net-udp"], "Retransmit what was lost, in order, exactly once — approximately.", { src: ["stanford-cs144", "rfc9293"], obj: ["Explain reliable delivery as retransmitting what was lost so the receiver sees each byte once, in order — approximately", "Predict what sequence numbers and acknowledgements let a sender decide to retransmit"] }),
        concept("net-tcp-state", "TCP state machine", "net-t-rel", 2, ["net-reliable"], "LISTEN to TIME_WAIT as a protocol, not a diagram to memorize.", { src: ["stanford-cs144", "rfc9293"], obj: ["Describe TCP connection states as protocol rules, not a diagram to memorize", "Predict why TIME_WAIT exists after a close"] }),
        concept("net-handshake", "The handshake", "net-t-rel", 2, ["net-tcp-state"], "Three packets to agree a starting sequence.", { src: ["stanford-cs144", "rfc9293"], obj: ["Explain the three-way handshake as agreeing a starting sequence in each direction", "Predict what is known after SYN, SYN-ACK, and ACK that was not known before"] }),
        concept("net-sliding-window", "Sliding windows", "net-t-rel", 3, ["net-reliable", "net-bandwidth-delay"], "Many packets in flight, one window to bound them.", { src: ["stanford-cs144", "rfc9293"], obj: ["Describe a sliding window as many packets in flight bounded by one window", "Predict how much data can be in flight given a window and a bandwidth-delay product"] }),
        concept("net-flow-control", "Flow control", "net-t-rel", 3, ["net-sliding-window"], "The receiver advertising how much it can still take.", { src: ["stanford-cs144", "rfc9293"], obj: ["Explain flow control as the receiver advertising how much it can still take", "Distinguish a receiver that is full from a path that is congested"] }),
        concept("net-timeout", "Retransmit timers", "net-t-rel", 3, ["net-reliable"], "When silence means loss.", { src: ["stanford-cs144", "rfc9293"], obj: ["Explain a retransmit timer as treating silence as loss", "Predict the cost of a timer that is much shorter or much longer than the real RTT"] }),
        concept("net-fast-retransmit", "Fast retransmit", "net-t-rel", 3, ["net-timeout"], "Triple-duplicate ACK as an earlier hint than a timeout.", { src: ["stanford-cs144", "rfc5681"], obj: ["Describe fast retransmit as using triple-duplicate ACKs as an earlier loss hint than a timeout", "Predict why duplicate ACKs appear when a packet is lost but later packets arrive"] }),
        concept("net-nagle", "Nagle and delayed ACK", "net-t-rel", 3, ["net-tcp-state"], "Two optimizations that deadlock each other.", { src: ["stanford-cs144", "rfc9293"], obj: ["Explain Nagle and delayed ACK as two optimizations that can stall each other", "Predict a deadlock-like pause when both a small send and a delayed ACK wait for the other"] }),
        concept("net-keepalive", "Keepalives", "net-t-rel", 2, ["net-tcp-state"], "Probing a quiet connection so middleboxes do not forget it.", { src: ["stanford-cs144", "rfc9293"], obj: ["Describe a keepalive as probing a quiet connection so the path does not forget it", "Predict why a long-idle connection can vanish without keepalives"] }),
        concept("net-congestion", "Congestion control", "net-t-cc", 2, ["net-stack"], "How senders share a pipe they cannot see.", { src: ["stanford-cs144", "rfc5681"], obj: ["Explain congestion control as how senders share a pipe they cannot see", "Predict why a sender must infer path capacity from feedback rather than being told"] }),
        concept("net-aimd", "AIMD", "net-t-cc", 3, ["net-congestion"], "Additive increase, multiplicative decrease — fairness from a rule.", { src: ["stanford-cs144", "rfc5681"], obj: ["Describe AIMD as additive increase and multiplicative decrease", "Predict how two senders using the same AIMD rule share a bottleneck"] }),
        concept("net-slow-start", "Slow start", "net-t-cc", 3, ["net-aimd"], "Exponential probing until you hit something.", { src: ["stanford-cs144", "rfc5681"], obj: ["Explain slow start as exponential probing until a signal is hit", "Predict why slow start grows the window faster than AIMD's additive increase"] }),
        concept("net-rtt", "RTT estimation", "net-t-cc", 3, ["net-timeout"], "Jacobson/Karels: the mean is not enough; you need the variance.", { src: ["stanford-cs144", "rfc9293"], obj: ["Explain RTT estimation as needing both a mean and a variance", "Predict why a timeout based only on the mean is too aggressive when delay varies"] }),
        concept("net-fairness-tcp", "TCP fairness", "net-t-cc", 3, ["net-aimd"], "Equal shares among equal RTTs — and the bias when they are not.", { src: ["stanford-cs144", "rfc5681"], obj: ["Describe TCP fairness as equal shares among equal RTTs", "Predict the bias when two flows share a bottleneck but have different RTTs"] }),
        concept("net-head-of-line", "Head-of-line blocking", "net-t-cc", 3, ["net-reliable"], "One lost packet stalling the streams behind it.", { src: ["stanford-cs144"], obj: ["Explain head-of-line blocking as one lost packet stalling the streams behind it", "Predict why a reliable ordered byte stream delays later independent messages"] }),
        concept("net-bufferbloat", "Bufferbloat", "net-t-mod", 3, ["net-congestion"], "Huge buffers turning loss into delay.", { src: ["stanford-cs144"], obj: ["Describe bufferbloat as huge buffers turning congestion into delay instead of honest loss", "Predict why a latency-sensitive flow suffers in a standing queue even when the link is not dropping"] }),
        concept("net-ecn", "Explicit congestion notification", "net-t-mod", 4, ["net-congestion"], "Marking instead of dropping.", { src: ["stanford-cs144"], obj: ["Explain ECN as marking a packet instead of dropping it", "Predict what a sender should do when it sees a congestion mark"] }),
        concept("net-bbr", "Delay-based control (BBR)", "net-t-mod", 4, ["net-congestion", "net-bandwidth-delay"], "Model the pipe instead of waiting for a drop.", { src: ["stanford-cs144"], obj: ["Describe delay-based control as modeling the pipe instead of waiting for a drop", "Contrast a loss-based reaction with one that uses delay and delivery rate"] }),
        concept("net-quic", "QUIC", "net-t-mod", 4, ["net-udp", "net-head-of-line"], "Transport over UDP so we can change it without the kernel.", { src: ["stanford-cs144", "rfc9000"], obj: ["Explain QUIC as a transport over UDP so it can change without the kernel", "Predict how QUIC avoids head-of-line blocking across independent streams"] }),
        concept("net-tls-place", "Where TLS sits", "net-t-mod", 3, ["net-tcp-state"], "Encryption as a layer, and why QUIC ate it.", { src: ["stanford-cs144", "rfc9000"], obj: ["Locate TLS as encryption sitting on a byte stream, and say why QUIC absorbed it", "Predict what a middlebox that only understands TCP plus TLS cannot see inside QUIC"] }),
        concept("net-udp-use", "When UDP is the right tool", "net-t-mod", 3, ["net-udp", "net-congestion"], "Media, games, and anything that would rather drop than stall.", { src: ["stanford-cs144"], obj: ["Explain when UDP is the right tool: anything that would rather drop than stall", "Predict why a media sender often prefers a late packet dropped to a reliable stall"] }),
        concept("net-middlebox", "Middleboxes", "net-t-mod", 4, ["net-nat-intro", "net-tcp-state"], "The path is not just routers. It rewrites you.", { src: ["stanford-cs144"], obj: ["Describe a middlebox as a device on the path that rewrites or filters, not just a router", "Predict why an unexpected header or a quiet flow can be dropped or rewritten"] }),
      ],
    },
    {
      id: "net-internet",
      title: "Routing and Internet Architecture",
      categoryId: "networking",
      description: "How packets find a path across independently owned networks: prefixes, IGPs, BGP, peering, and the failures that are political.",
      curriculumVersion: 1,
      orderHint: 2,
      difficultyRange: [3, 5],
      entryRequirements: ["net-cidr", "net-ip"],
      sourceReferences: [
        { id: "stanford-cs144", title: "Stanford CS144", url: "https://web.stanford.edu/class/cs144/", kind: "syllabus", notes: "Internet architecture after a working transport. BGP is policy, not a shortest-path toy." },
        { id: "rfc4271", title: "A Border Gateway Protocol 4 (BGP-4)", url: "https://www.rfc-editor.org/rfc/rfc4271", kind: "notes", notes: "Path-vector reachability plus attributes. Policy, not shortest path." },
        { id: "rfc1034", title: "Domain Names — Concepts and Facilities", url: "https://www.rfc-editor.org/rfc/rfc1034", kind: "notes", notes: "Hierarchy, resolvers, and caches as a system." },
      ],
      modules: [
        { id: "net-i-fwd", title: "Forwarding", blurb: "Longest prefix, tables.", order: 0, prerequisites: [], spineIds: ["net-forwarding-vs-routing"], learningObjectives: ["Separate the datapath lookup from the process that filled the table", "Let the more specific prefix win"] },
        { id: "net-i-igp", title: "Inside an AS", blurb: "IGPs, OSPF.", order: 1, prerequisites: ["net-i-fwd"], spineIds: ["net-igp"], learningObjectives: ["Treat an AS as one routing policy", "Flood a map inside the AS, then run shortest path"] },
        { id: "net-i-bgp", title: "Between ASes", blurb: "BGP, peering, hijacks.", order: 2, prerequisites: ["net-i-igp"], spineIds: ["net-bgp"], learningObjectives: ["Route between ASes by policy, not shortest path", "Name hijacks and leaks as failures of that policy"] },
        { id: "net-i-sys", title: "The system around it", blurb: "DNS, anycast, CDN, IPv6.", order: 3, prerequisites: ["net-i-bgp"], spineIds: ["net-dns-system", "net-anycast"], learningObjectives: ["Treat DNS, anycast, and CDNs as systems that ride the routing plane", "Measure and fail in ways that are political as well as technical"] },
      ],
      concepts: [
        concept("net-forwarding-vs-routing", "Forwarding versus routing", "net-i-fwd", 3, ["net-cidr"], "The lookup in the datapath versus the process that filled the table.", { src: ["stanford-cs144"], obj: ["Distinguish forwarding (the datapath lookup) from routing (the process that filled the table)", "Predict what still works if the routing process dies but the forwarding table remains"] }),
        concept("net-longest-prefix", "Longest-prefix match", "net-i-fwd", 3, ["net-forwarding-vs-routing"], "More specific wins.", { src: ["stanford-cs144"], obj: ["Explain longest-prefix match as the more specific prefix winning", "Apply two overlapping prefixes to decide which next hop a destination takes"] }),
        concept("net-as", "Autonomous systems", "net-i-igp", 3, ["net-ip"], "A network with one routing policy.", { src: ["stanford-cs144"], obj: ["Describe an autonomous system as a network with one routing policy", "Predict why two networks that share a cable still need a protocol if they have different policies"] }),
        concept("net-igp", "Interior gateway protocols", "net-i-igp", 3, ["net-as"], "How an AS talks to itself.", { src: ["stanford-cs144"], obj: ["Explain an IGP as how an AS computes paths inside itself", "Distinguish an interior protocol from the protocol used between ASes"] }),
        concept("net-ospf", "Link-state routing (OSPF)", "net-i-igp", 4, ["net-igp"], "Everyone floods the map, then runs shortest path.", { src: ["stanford-cs144"], obj: ["Describe link-state routing as flooding a map and then running shortest path", "Predict what each router recomputes after a link-state advertisement arrives"] }),
        concept("net-bgp", "BGP", "net-i-bgp", 3, ["net-as"], "Policy routing between autonomous systems.", { src: ["stanford-cs144", "rfc4271"], obj: ["Explain BGP as policy routing between autonomous systems, not shortest path", "Predict why a longer AS path can still be chosen when policy prefers it"] }),
        concept("net-peering", "Peering", "net-i-bgp", 4, ["net-bgp"], "Settlement-free exchange, until it is not.", { src: ["stanford-cs144", "rfc4271"], obj: ["Describe peering as settlement-free exchange of customer routes", "Predict why a peer will not typically offer transit to the rest of the Internet"] }),
        concept("net-transit", "Transit", "net-i-bgp", 4, ["net-bgp"], "Paying someone to reach the rest of the Internet.", { src: ["stanford-cs144", "rfc4271"], obj: ["Describe transit as paying another AS to reach the rest of the Internet", "Contrast a transit link with a settlement-free peer"] }),
        concept("net-ixp", "Internet exchange points", "net-i-bgp", 4, ["net-peering"], "A room where many ASes meet.", { src: ["stanford-cs144"], obj: ["Describe an IXP as a fabric where many ASes meet to peer", "Predict why an exchange reduces the need to buy transit for some paths"] }),
        concept("net-policy-route", "Policy in the path", "net-i-bgp", 4, ["net-bgp"], "Local preference and AS-path as politics encoded in attributes.", { src: ["stanford-cs144", "rfc4271"], obj: ["Explain local preference and AS-path as policy encoded in attributes", "Predict which attribute typically decides a route before AS-path length does"] }),
        concept("net-hijack", "Route hijacks", "net-i-bgp", 4, ["net-bgp"], "Announcing a prefix you do not own.", { src: ["stanford-cs144", "rfc4271"], obj: ["Describe a route hijack as announcing a prefix you do not own", "Predict why a more-specific unauthorized prefix can steal traffic"] }),
        concept("net-rpki", "RPKI", "net-i-bgp", 5, ["net-hijack"], "Cryptographic permission to announce a prefix.", { src: ["stanford-cs144"], obj: ["Explain RPKI as cryptographic permission for an AS to originate a prefix", "Predict what origin validation does and does not stop"] }),
        concept("net-dns-system", "The DNS as a system", "net-i-sys", 3, ["net-dns-intro"], "Hierarchy, caches, and the resolvers in the middle.", { src: ["stanford-cs144", "rfc1034"], obj: ["Describe the DNS as a hierarchy of zones, caches, and resolvers", "Trace a name from a stub resolver through caches to an authoritative server"] }),
        concept("net-anycast", "Anycast", "net-i-sys", 4, ["net-bgp"], "One address, many locations, routing decides which.", { src: ["stanford-cs144"], obj: ["Explain anycast as one address announced from many places, with routing choosing", "Predict why two clients can reach different locations for the same address"] }),
        concept("net-anycast-dns", "Anycast DNS", "net-i-sys", 4, ["net-anycast", "net-dns-system"], "Why 8.8.8.8 is near everyone.", { src: ["stanford-cs144"], obj: ["Explain anycast DNS as putting the same resolver address near many clients", "Predict why 8.8.8.8 can be a short hop from almost anywhere"] }),
        concept("net-cdn", "CDNs", "net-i-sys", 4, ["net-anycast", "net-dns-system"], "Putting the bytes next to the viewer.", { src: ["stanford-cs144"], obj: ["Describe a CDN as putting bytes next to the viewer using DNS and/or anycast", "Predict why a cache miss at the edge still has to fetch from an origin"] }),
        concept("net-ipv6", "IPv6", "net-i-sys", 3, ["net-ip"], "Larger addresses and a different neighbor-discovery story.", { src: ["stanford-cs144"], obj: ["Describe IPv6 as larger addresses and a neighbor-discovery story instead of ARP", "Predict what a host uses to find a neighbor's link address on IPv6"] }),
        concept("net-mpls", "MPLS", "net-i-sys", 4, ["net-forwarding-vs-routing"], "Labels instead of IP lookups in the core.", { src: ["stanford-cs144"], obj: ["Explain MPLS as forwarding on labels instead of repeating an IP lookup in the core", "Predict what a core router looks at when a labeled packet arrives"] }),
        concept("net-sdn", "Software-defined control", "net-i-sys", 4, ["net-forwarding-vs-routing"], "A logically centralized controller filling the tables.", { src: ["stanford-cs144"], obj: ["Describe SDN as a logically centralized controller filling forwarding tables", "Contrast a distributed control plane with a controller that computes paths"] }),
        concept("net-measurement", "Measuring the Internet", "net-i-sys", 4, ["net-icmp", "net-bgp"], "Traceroute lies, and we use it anyway.", { src: ["stanford-cs144"], obj: ["Explain why traceroute can lie and why we still use it", "Predict a case where ICMP replies do not reveal the true forwarding path"] }),
        concept("net-anycast-http", "Anycast at L7", "net-i-sys", 4, ["net-cdn"], "When the TCP connection and the HTTP cache disagree about where you are.", { src: ["stanford-cs144"], obj: ["Explain the clash when a TCP connection and an HTTP cache disagree about where the client is", "Predict what breaks if routing moves an anycast TCP flow to a different site mid-connection"] }),
        concept("net-anycast-bgp", "BGP and anycast interaction", "net-i-sys", 5, ["net-anycast", "net-policy-route"], "A more-specific prefix pulling traffic across an ocean.", { src: ["stanford-cs144", "rfc4271"], obj: ["Explain how a more-specific prefix can pull anycast traffic across an ocean", "Predict why policy and prefix length can override nearest for an anycast address"] }),
      ],
    },
    {
      id: "cmp-frontend",
      title: "Compiler Front-End",
      categoryId: "compilers",
      description: "From characters to a typed tree: lexing, parsing, names, and the errors a compiler owes the human.",
      curriculumVersion: 1,
      orderHint: 0,
      difficultyRange: [0, 3],
      entryRequirements: [],
      sourceReferences: [
        { id: "stanford-cs143", title: "Stanford CS143 Compilers", url: "https://web.stanford.edu/class/cs143/", kind: "syllabus", notes: "Lex, parse, semantic analysis as the first project phases. Optimization waits." },
        { id: "dragon-book", title: "Compilers: Principles, Techniques, and Tools", kind: "textbook", notes: "Regular lexers, CFGs, and the source-to-tree pipeline. Mechanism, not theorem lists." },
      ],
      modules: [
        { id: "cmp-fe-lex", title: "Lexical analysis", blurb: "Characters into tokens.", order: 0, prerequisites: [], spineIds: ["cmp-front"], learningObjectives: ["Recover words from characters", "Keep a span so a diagnostic can point"] },
        { id: "cmp-fe-parse", title: "Parsing", blurb: "Tokens into a tree.", order: 1, prerequisites: ["cmp-fe-lex"], spineIds: ["cmp-cfg", "cmp-ast"], learningObjectives: ["Recover nested structure from a token stream", "Drop punctuation into the tree typechecking will walk"] },
        { id: "cmp-fe-sem", title: "Names and types", blurb: "The tree becomes honest.", order: 2, prerequisites: ["cmp-fe-parse"], spineIds: ["cmp-typecheck"], learningObjectives: ["Bind each use to a declaration", "Reject a tree whose operators cannot agree"] },
      ],
      concepts: [
        concept("cmp-front", "Lexing and parsing", "cmp-fe-lex", 0, [], "Turning characters into a tree the rest of the compiler can hate.", { src: ["stanford-cs143", "dragon-book"], obj: ["Trace source text through lexing and parsing into a tree later stages can consume", "Name what errors the front end can report before any IR exists"] }),
        concept("cmp-token", "Tokens", "cmp-fe-lex", 1, ["cmp-front"], "The words of a programming language.", { src: ["stanford-cs143", "dragon-book"], parent: "cmp-front", obj: ["Describe a token as a classified lexeme plus the payload later stages need", "Split a short source fragment into the tokens a parser would see"] }),
        concept("cmp-regex-lex", "Regular lexers", "cmp-fe-lex", 1, ["cmp-token"], "Why identifiers and numbers are regular while nested expressions are not.", { src: ["stanford-cs143", "dragon-book"], parent: "cmp-token", obj: ["Explain why identifiers and numbers are regular while nested expressions are not", "Predict which lexeme a maximal-munch lexer accepts at a given position"] }),
        concept("cmp-source-loc", "Source locations", "cmp-fe-lex", 1, ["cmp-token"], "Every diagnostic needs a span.", { src: ["stanford-cs143"], parent: "cmp-token", obj: ["Attach a source span to a token so a diagnostic can point at the original text", "Predict what a diagnostic loses if tokens forget where they came from"] }),
        concept("cmp-preproc", "Preprocessing", "cmp-fe-lex", 2, ["cmp-token"], "A language before the language.", { src: ["stanford-cs143"], parent: "cmp-token", obj: ["Describe preprocessing as a rewrite of the token stream before the language parser runs", "Name an error that appears only after expansion, not in the raw source"] }),
        concept("cmp-macro", "Macros", "cmp-fe-lex", 2, ["cmp-preproc"], "Expansion that later phases must still explain.", { src: ["stanford-cs143"], parent: "cmp-preproc", obj: ["Trace a macro expansion and the token stream the parser actually sees", "Explain why a later diagnostic must still mention the use site, not only the expansion"] }),
        concept("cmp-cfg", "Context-free grammars", "cmp-fe-parse", 1, ["cmp-front"], "Recursive structure you can write down.", { src: ["stanford-cs143", "dragon-book"], obj: ["Write a small context-free grammar that generates nested expressions", "Distinguish a regular token language from a context-free phrase structure"] }),
        concept("cmp-parse-tree", "Parse trees", "cmp-fe-parse", 1, ["cmp-cfg"], "The derivation, including the punctuation.", { src: ["stanford-cs143", "dragon-book"], parent: "cmp-cfg", obj: ["Build the concrete parse tree for a short derivation, including punctuation", "Name what the parse tree still contains that later stages do not need"] }),
        concept("cmp-ast", "Abstract syntax trees", "cmp-fe-parse", 1, ["cmp-parse-tree"], "The tree you actually want to typecheck.", { src: ["stanford-cs143", "dragon-book"], parent: "cmp-parse-tree", obj: ["Drop concrete punctuation from a parse tree to obtain the AST typechecking will walk", "Distinguish a parse tree from an AST on the same source fragment"] }),
        concept("cmp-recursive-descent", "Recursive descent", "cmp-fe-parse", 2, ["cmp-cfg"], "One function per nonterminal.", { src: ["stanford-cs143", "dragon-book"], parent: "cmp-cfg", obj: ["Trace one function-per-nonterminal parse of a short token stream", "Predict where a recursive-descent parser fails on left recursion"] }),
        concept("cmp-lr", "Bottom-up parsing", "cmp-fe-parse", 2, ["cmp-cfg"], "Shift and reduce, and the tables that drive them.", { src: ["stanford-cs143", "dragon-book"], parent: "cmp-cfg", obj: ["Trace a short shift/reduce sequence on a tiny grammar", "Diagnose a shift/reduce conflict as two legal actions on the same lookahead"] }),
        concept("cmp-ambiguity", "Ambiguity", "cmp-fe-parse", 2, ["cmp-cfg"], "Two trees for one string.", { src: ["stanford-cs143", "dragon-book"], parent: "cmp-cfg", obj: ["Exhibit two different trees for one string under an ambiguous grammar", "Resolve a classic ambiguity with precedence or a rewritten production"] }),
        concept("cmp-grammar-class", "LL versus LR", "cmp-fe-parse", 3, ["cmp-recursive-descent", "cmp-lr"], "What each algorithm will not swallow.", { src: ["stanford-cs143", "dragon-book"], obj: ["Predict a construct LL cannot swallow that LR still can", "Compare what left recursion and left factoring do to each algorithm"] }),
        concept("cmp-semantic-action", "Semantic actions", "cmp-fe-parse", 2, ["cmp-ast"], "Building values while you parse.", { src: ["stanford-cs143"], parent: "cmp-ast", obj: ["Describe a semantic action as a value built while a production is recognized", "Predict the AST node a reduction or a descent return constructs"] }),
        concept("cmp-error-recovery", "Parse error recovery", "cmp-fe-parse", 3, ["cmp-recursive-descent"], "Reporting more than the first mistake.", { src: ["stanford-cs143"], parent: "cmp-recursive-descent", obj: ["Explain how panic-mode recovery reports more than the first bad token", "Predict where a recursive-descent parser resynchronizes after a missing semicolon"] }),
        concept("cmp-symbol-table", "Symbol tables", "cmp-fe-sem", 2, ["cmp-ast"], "A map from names to meanings, stacked by scope.", { src: ["stanford-cs143", "dragon-book"], parent: "cmp-ast", obj: ["Describe a symbol table as a stacked map from names to declarations", "Predict what a lookup returns after a new scope is pushed"] }),
        concept("cmp-scope", "Scope", "cmp-fe-sem", 2, ["cmp-symbol-table"], "Where a name is visible.", { src: ["stanford-cs143", "dragon-book"], parent: "cmp-symbol-table", obj: ["State where a name is visible and where a hole or shadow hides it", "Predict which declaration an inner use sees when an outer name is shadowed"] }),
        concept("cmp-name-resolve", "Name resolution", "cmp-fe-sem", 2, ["cmp-scope"], "Which declaration this use refers to.", { src: ["stanford-cs143"], parent: "cmp-scope", obj: ["Bind a use to exactly one declaration using the active scope stack", "Diagnose an unbound name as a resolution failure, not a parse failure"] }),
        concept("cmp-typecheck", "Type checking", "cmp-fe-sem", 2, ["cmp-name-resolve"], "Rejecting trees that cannot mean anything.", { src: ["stanford-cs143", "dragon-book"], parent: "cmp-name-resolve", obj: ["Reject a well-parsed tree whose operators and operands cannot agree", "Predict the type of a small expression after names have been resolved"] }),
        concept("cmp-overload", "Overload resolution", "cmp-fe-sem", 3, ["cmp-typecheck"], "Picking which f this f is.", { src: ["stanford-cs143"], parent: "cmp-typecheck", obj: ["Pick the unique viable candidate for a call given argument types", "Diagnose an ambiguous call as more than one equally good candidate"] }),
        concept("cmp-visitor", "Walking the tree", "cmp-fe-sem", 2, ["cmp-ast"], "Visitors and the passes that share them.", { src: ["stanford-cs143"], parent: "cmp-ast", obj: ["Walk an AST with a visitor so several passes share one traversal shape", "Name what a later pass may assume after an earlier walk has finished"] }),
        concept("cmp-ir-lower-intro", "Lowering, first look", "cmp-fe-sem", 3, ["cmp-typecheck"], "The typed tree is not yet something you can allocate registers for.", { src: ["stanford-cs143", "cmu-15411"], parent: "cmp-typecheck", obj: ["Explain why a typed tree is still not a form you can allocate registers for", "Name what lowering must make explicit before three-address code exists"] }),
      ],
    },
    {
      id: "cmp-ir",
      title: "Intermediate Representations and Optimisation",
      categoryId: "compilers",
      description: "SSA, dataflow, and the passes that change the program without changing what it means.",
      curriculumVersion: 1,
      orderHint: 1,
      difficultyRange: [2, 4],
      entryRequirements: ["cmp-typecheck", "cmp-ast"],
      sourceReferences: [
        { id: "stanford-cs143", title: "Stanford CS143", url: "https://web.stanford.edu/class/cs143/", kind: "syllabus", notes: "IR and local/global optimization after the front-end exists." },
        { id: "cmu-15411", title: "CMU 15-411/611 Compiler Design", url: "https://csd.cs.cmu.edu/15411611-compiler-design", kind: "syllabus", notes: "IR and dataflow as the intellectual center." },
        { id: "llvm-langref", title: "LLVM Language Reference Manual", url: "https://llvm.org/docs/LangRef.html", kind: "vendor", notes: "SSA, φ-nodes, and IR well-formedness as a contract." },
      ],
      modules: [
        { id: "cmp-ir-form", title: "IR shapes", blurb: "Three-address, CFG, SSA.", order: 0, prerequisites: [], spineIds: ["cmp-ssa"], learningObjectives: ["Name values so each assignment happens once", "Split a program into blocks and edges"] },
        { id: "cmp-ir-flow", title: "Dataflow", blurb: "Liveness and friends.", order: 1, prerequisites: ["cmp-ir-form"], spineIds: ["cmp-dataflow"], learningObjectives: ["Compute a fact that flows until it settles", "Say which values are still needed"] },
        { id: "cmp-ir-opt", title: "Transformations", blurb: "The useful rewrites.", order: 2, prerequisites: ["cmp-ir-flow"], spineIds: ["cmp-dce", "cmp-inlining"], learningObjectives: ["Delete work nothing uses", "Replace a call with a body only when the price is worth it"] },
      ],
      concepts: [
        concept("cmp-three-addr", "Three-address code", "cmp-ir-form", 2, ["cmp-ir-lower-intro"], "One operator, a few operands, easy to move.", { src: ["stanford-cs143", "cmu-15411", "dragon-book"], obj: ["Rewrite a nested expression into three-address instructions with temporary names", "Explain why one operator and a few operands make later motion easy"] }),
        concept("cmp-cfg-ir", "Control-flow graphs", "cmp-ir-form", 2, ["cmp-three-addr"], "Basic blocks and the edges between them.", { src: ["stanford-cs143", "cmu-15411"], parent: "cmp-three-addr", obj: ["Split three-address code into basic blocks and the edges between them", "Identify a basic block as a straight-line sequence with one entry and one exit"] }),
        concept("cmp-ssa", "SSA form", "cmp-ir-form", 2, ["cmp-front"], "Single assignment so dataflow becomes a graph problem.", { src: ["cmu-15411", "llvm-langref"], obj: ["Rewrite assignments so each name is defined exactly once", "Explain why a single definition per name turns use-def into a graph walk"] }),
        concept("cmp-phi", "φ-nodes", "cmp-ir-form", 3, ["cmp-ssa", "cmp-cfg-ir"], "A merge of names at a join point.", { src: ["cmu-15411", "llvm-langref"], obj: ["Place a φ at a join so one name records which predecessor's value arrives", "Predict the φ operands for a variable assigned on two incoming edges"] }),
        concept("cmp-mem2reg", "Memory to registers", "cmp-ir-form", 3, ["cmp-ssa"], "Promoting allocas so SSA can see them.", { src: ["llvm-langref"], parent: "cmp-ssa", obj: ["Promote an alloca that never escapes into an SSA value", "Name a reason a stack slot must stay in memory instead of becoming a register"] }),
        concept("cmp-ir-verify", "IR verification", "cmp-ir-form", 3, ["cmp-ssa"], "Types and dominance as a checker, not a hope.", { src: ["llvm-langref"], parent: "cmp-ssa", obj: ["Check that every use is dominated by its definition and that types agree", "Diagnose an ill-formed SSA fragment the verifier should reject"] }),
        concept("cmp-dataflow", "Dataflow analysis", "cmp-ir-flow", 3, ["cmp-cfg-ir"], "Facts that flow forward or backward until they settle.", { src: ["cmu-15411", "dragon-book"], obj: ["Describe a dataflow fact as something that flows until a fixed point", "Predict whether a given analysis must run forward or backward"] }),
        concept("cmp-liveness", "Liveness", "cmp-ir-flow", 3, ["cmp-dataflow"], "Which values are still needed.", { src: ["cmu-15411", "dragon-book"], parent: "cmp-dataflow", obj: ["Compute which names are live on entry to a short block", "Predict why a value that is never used is not live"] }),
        concept("cmp-alias", "Alias analysis", "cmp-ir-flow", 4, ["cmp-dataflow"], "May these two pointers be the same.", { src: ["cmu-15411", "llvm-langref"], parent: "cmp-dataflow", obj: ["Decide whether two pointers may designate the same location", "Name a transformation that becomes illegal if they may alias"] }),
        concept("cmp-call-graph", "Call graphs", "cmp-ir-flow", 3, ["cmp-cfg-ir"], "Who might call whom.", { src: ["cmu-15411"], parent: "cmp-cfg-ir", obj: ["Build a call graph from the calls that might execute", "Predict how an indirect call makes the graph conservative"] }),
        concept("cmp-pass-manager", "Pass managers", "cmp-ir-flow", 3, ["cmp-ir-verify"], "Order, invalidation, and why passes fight.", { src: ["llvm-langref", "gcc-internals"], parent: "cmp-ir-verify", obj: ["Explain why one pass can invalidate another pass's analysis", "Predict a safe order when a transform dirties SSA or alias facts"] }),
        concept("cmp-const-fold", "Constant folding", "cmp-ir-opt", 3, ["cmp-ssa"], "Doing arithmetic the compiler can already see.", { src: ["stanford-cs143", "cmu-15411"], parent: "cmp-ssa", obj: ["Replace an operator whose operands are already constants with the result", "State why folding 2+3 does not change observable behavior"] }),
        concept("cmp-dce", "Dead code elimination", "cmp-ir-opt", 3, ["cmp-liveness"], "Deleting what nothing uses.", { src: ["stanford-cs143", "cmu-15411"], parent: "cmp-liveness", obj: ["Delete an instruction whose result is not live", "Predict when a store cannot be deleted even if its value looks unused"] }),
        concept("cmp-cse", "Common-subexpression elimination", "cmp-ir-opt", 3, ["cmp-ssa"], "Computing a thing once.", { src: ["stanford-cs143", "cmu-15411"], parent: "cmp-ssa", obj: ["Reuse an earlier computation of the same expression when values have not changed", "Diagnose when two similar expressions are not actually common"] }),
        concept("cmp-inlining", "Inlining", "cmp-ir-opt", 3, ["cmp-call-graph"], "Replacing a call with the body, for a price.", { src: ["cmu-15411"], parent: "cmp-call-graph", obj: ["Replace a call with the callee body and state the compile-time price", "Predict a case where inlining is legal but unprofitable"] }),
        concept("cmp-loop-inv", "Loop-invariant code motion", "cmp-ir-opt", 4, ["cmp-cfg-ir"], "Hoisting work the loop does not change.", { src: ["cmu-15411"], parent: "cmp-cfg-ir", obj: ["Hoist a computation the loop does not change to the preheader", "Identify the loop-invariant pattern a pass looks for"] }),
        concept("cmp-licm", "LICM carefully", "cmp-ir-opt", 4, ["cmp-loop-inv", "cmp-alias"], "When a load is not actually invariant.", { src: ["cmu-15411", "llvm-langref"], obj: ["Refuse to hoist a load that may alias a store in the loop", "Diagnose a hoist that would change which exception or fault is observed"] }),
        concept("cmp-strength-red", "Strength reduction", "cmp-ir-opt", 3, ["cmp-loop-inv"], "A multiply becoming an add.", { src: ["cmu-15411", "dragon-book"], parent: "cmp-loop-inv", obj: ["Replace an induction multiply with an add that steps the same sequence", "State why the new recurrence still computes the original values"] }),
        concept("cmp-sroa", "Scalar replacement of aggregates", "cmp-ir-opt", 4, ["cmp-mem2reg"], "Exploding a struct into pieces SSA can eat.", { src: ["llvm-langref"], parent: "cmp-mem2reg", obj: ["Split an aggregate into scalar pieces SSA can name", "Predict when a field still cannot be promoted because its address escapes"] }),
        concept("cmp-tailcall", "Tail calls", "cmp-ir-opt", 3, ["cmp-cfg-ir"], "A call that can be a jump.", { src: ["cmu-15411"], parent: "cmp-cfg-ir", obj: ["Rewrite a call in tail position as a jump that reuses the current frame", "Diagnose a call that is not tail because work remains after it returns"] }),
        concept("cmp-vectorize", "Vectorization", "cmp-ir-opt", 4, ["cmp-loop-inv", "cpu-vector-simd"], "One instruction, many loop iterations.", { src: ["cmu-15411"], obj: ["Pack consecutive independent iterations into one SIMD instruction", "Name a dependence or alias fact that makes vectorization illegal"] }),
        concept("cmp-ipo", "Interprocedural optimization", "cmp-ir-opt", 4, ["cmp-inlining", "cmp-call-graph"], "Facts that cross function boundaries.", { src: ["cmu-15411"], obj: ["Propagate a fact across a call boundary the inliner has not removed", "Compare a local fact with one that is only true after seeing the callee"] }),
      ],
    },
    {
      id: "cmp-backend",
      title: "Code Generation and Runtime Systems",
      categoryId: "compilers",
      description: "Instruction selection, register allocation, ABIs, and the runtime that still exists after the compiler is done — including JIT.",
      curriculumVersion: 1,
      orderHint: 2,
      difficultyRange: [3, 5],
      entryRequirements: ["cmp-ssa", "cmp-liveness"],
      sourceReferences: [
        { id: "stanford-cs143", title: "Stanford CS143", url: "https://web.stanford.edu/class/cs143/", kind: "syllabus", notes: "Codegen, register allocation, GC, JIT as later phases." },
        { id: "cmu-15411", title: "CMU 15-411/611", url: "https://csd.cs.cmu.edu/15411611-compiler-design", kind: "syllabus", notes: "Instruction selection and the backend as first-class work." },
        { id: "sysv-abi", title: "System V AMD64 ABI", url: "https://gitlab.com/x86-psABIs/x86-64-ABI", kind: "notes", notes: "One concrete calling convention and frame layout." },
        { id: "jvm-spec", title: "Java Virtual Machine Specification", url: "https://docs.oracle.com/javase/specs/jvms/se21/html/index.html", kind: "notes", notes: "Runtime, GC roots, and compiling while the program already runs." },
      ],
      modules: [
        { id: "cmp-be-sel", title: "Selection and allocation", blurb: "ISel, coloring, spilling.", order: 0, prerequisites: [], spineIds: ["cmp-alloc", "cmp-isel"], learningObjectives: ["Turn an IR operator into target opcodes", "Pack live values into the real register file"] },
        { id: "cmp-be-abi", title: "Talking to the platform", blurb: "Calls, frames, objects.", order: 1, prerequisites: ["cmp-be-sel"], spineIds: ["cmp-abi-call"], learningObjectives: ["Place arguments where the platform requires", "Leave a frame the callee and the unwinder can both read"] },
        { id: "cmp-be-rt", title: "Runtimes and JITs", blurb: "GC, exceptions, dynamic compilation.", order: 2, prerequisites: ["cmp-be-abi"], spineIds: ["cmp-gc", "cmp-jit"], learningObjectives: ["Find live objects without a matching free()", "Compile a hot fragment while the program is already running"] },
      ],
      concepts: [
        concept("cmp-isel", "Instruction selection", "cmp-be-sel", 3, ["cmp-three-addr"], "IR operators become real opcodes.", { src: ["cmu-15411", "gcc-internals"], obj: ["Replace an IR operator with one or more target opcodes that implement it", "Compare two legal selections and say what makes one cheaper"] }),
        concept("cmp-alloc", "Register allocation", "cmp-be-sel", 3, ["cmp-ssa"], "Graph coloring, spilling, and the real cost of an extra live range.", { src: ["stanford-cs143", "cmu-15411"], obj: ["Assign interfering live ranges to different registers or to memory", "Predict when the interference graph forces a spill"] }),
        concept("cmp-spill", "Spilling", "cmp-be-sel", 4, ["cmp-alloc", "cmp-liveness"], "A value that did not fit, living on the stack.", { src: ["cmu-15411"], obj: ["Insert a store and later reload because no register is free", "Predict the extra memory traffic a spill adds to a hot loop"] }),
        concept("cmp-scheduling", "Instruction scheduling", "cmp-be-sel", 4, ["cmp-isel", "cpu-pipeline"], "Reordering to hide latency without breaking dependences.", { src: ["cmu-15411"], obj: ["Reorder independent instructions to hide latency without breaking dependences", "Diagnose a schedule that moves a load before the store it depends on"] }),
        concept("cmp-peephole", "Peephole opts", "cmp-be-sel", 3, ["cmp-isel"], "Tiny local rewrites on the real ISA.", { src: ["gcc-internals"], parent: "cmp-isel", obj: ["Rewrite a short window of target instructions into a cheaper equivalent", "State why a peephole must preserve the window's observable effect"] }),
        concept("cmp-abi-call", "Calling conventions", "cmp-be-abi", 3, ["cmp-isel", "cpu-isa"], "Which arguments live in which registers.", { src: ["sysv-abi", "stanford-cs143"], obj: ["Place arguments and the return value where the platform's convention requires", "Predict which registers a caller must save across a call"] }),
        concept("cmp-frame", "Stack frames", "cmp-be-abi", 3, ["cmp-abi-call"], "The record a call leaves behind.", { src: ["sysv-abi", "stanford-cs143"], parent: "cmp-abi-call", obj: ["Describe the stack frame a prologue builds and an epilogue tears down", "Trace where a spilled value and a return address sit in the frame"] }),
        concept("cmp-object", "Object files", "cmp-be-abi", 3, ["cmp-isel"], "Sections, symbols, and what the linker will see.", { src: ["stanford-cs143"], parent: "cmp-isel", obj: ["Name the sections and symbols an object file shows the linker", "Predict what is still unknown inside a .o that the linker must finish"] }),
        concept("cmp-reloc", "Relocations", "cmp-be-abi", 3, ["cmp-object"], "Addresses the linker still has to fill in.", { src: ["stanford-cs143"], parent: "cmp-object", obj: ["Describe a relocation as a hole the linker fills with a final address", "Predict what breaks if a relocation is applied at the wrong width or place"] }),
        concept("cmp-linker", "Linking", "cmp-be-abi", 3, ["cmp-reloc"], "Many objects, one address space.", { src: ["stanford-cs143"], parent: "cmp-reloc", obj: ["Combine object files by resolving symbols and applying relocations", "Diagnose an undefined reference as a symbol no object provided"] }),
        concept("cmp-pic", "Position-independent code", "cmp-be-abi", 4, ["cmp-reloc"], "Code that does not assume where it was loaded.", { src: ["sysv-abi"], parent: "cmp-reloc", obj: ["Generate an address using a relative load so the code can sit anywhere", "Predict why an absolute immediate is illegal in a shared library"] }),
        concept("cmp-tls", "Thread-local storage", "cmp-be-abi", 4, ["cmp-abi-call", "os-thread-vs-proc"], "A global that is not shared.", { src: ["sysv-abi"], obj: ["Describe thread-local storage as a global name with one cell per thread", "Predict what goes wrong if two threads share an ordinary global instead"] }),
        concept("cmp-debug-info", "Debug info", "cmp-be-abi", 3, ["cmp-source-loc"], "DWARF as a map back to the source the optimizer scrambled.", { src: ["stanford-cs143"], parent: "cmp-source-loc", obj: ["Map an optimized location back to a source span the human still recognizes", "Predict why a dead store's variable may have no current location"] }),
        concept("cmp-runtime", "The language runtime", "cmp-be-rt", 3, ["cmp-abi-call"], "The support code every compiled program still links.", { src: ["stanford-cs143", "jvm-spec"], parent: "cmp-abi-call", obj: ["Name support the compiled program still links after the compiler finishes", "Distinguish a language runtime from the OS kernel"] }),
        concept("cmp-gc", "Automatic memory management", "cmp-be-rt", 4, ["cmp-runtime"], "Finding the live objects without a free().", { src: ["stanford-cs143", "jvm-spec"], parent: "cmp-runtime", obj: ["Find live objects from roots without a matching free()", "Compare a conservative scan with a precise one that needs stack maps"] }),
        concept("cmp-exception", "Exceptions", "cmp-be-rt", 4, ["cmp-frame"], "Unwinding until someone catches.", { src: ["itanium-cxx-abi", "stanford-cs143"], parent: "cmp-frame", obj: ["Unwind frames until a matching handler is found", "Predict which destructors or cleanups run on the way out"] }),
        concept("cmp-unwinding", "Stack unwinding", "cmp-be-rt", 4, ["cmp-exception"], "Tables instead of a setjmp in every frame.", { src: ["itanium-cxx-abi"], parent: "cmp-exception", obj: ["Use unwind tables instead of a setjmp in every frame", "Explain why tables stay off the hot path until an exception is thrown"] }),
        concept("cmp-stackmap", "Stack maps", "cmp-be-rt", 5, ["cmp-gc", "cmp-jit"], "Where the GC roots are at this safepoint.", { src: ["jvm-spec", "llvm-langref"], obj: ["Record where GC roots live at a safepoint", "Predict what the collector gets wrong if a root is missing from the map"] }),
        concept("cmp-jit", "JIT compilation", "cmp-be-rt", 4, ["cmp-isel"], "Compiling while the program runs.", { src: ["stanford-cs143", "jvm-spec"], parent: "cmp-isel", obj: ["Compile a hot fragment while the program is already running", "Compare JIT compile-time cost with the speed the new code must repay"] }),
        concept("cmp-deopt", "Deoptimization", "cmp-be-rt", 5, ["cmp-jit"], "Falling back when a speculative type was a lie.", { src: ["jvm-spec"], parent: "cmp-jit", obj: ["Fall back to a safer version when a speculative assumption is false", "Predict the state the interpreter or baseline compiler must reconstruct"] }),
        concept("cmp-lto", "Link-time optimization", "cmp-be-rt", 4, ["cmp-ipo", "cmp-linker"], "Seeing the whole program after the .o files exist.", { src: ["llvm-langref", "gcc-internals"], obj: ["Optimize across object-file boundaries after the linker can see the whole program", "Compare LTO with a compile-time IPO that never saw the other .o"] }),
        concept("cmp-pgo", "Profile-guided optimization", "cmp-be-rt", 4, ["cmp-inlining"], "Letting yesterday's run tell today's inliner the truth.", { src: ["gcc-internals"], parent: "cmp-inlining", obj: ["Use yesterday's profile to decide today's inlining and layout", "Predict a mis-optimization when the profile does not match this run"] }),
      ],
    },
  ];
}
